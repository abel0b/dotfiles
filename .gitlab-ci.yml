stages:
    - test

test:
    stage: test
    image: archlinux/base-devel
    before_script:
        - pacman -Sy
        - touch /etc/sudoers
    script:
        - curl https://gitlab.com/abeliam/dotfiles/raw/master/bootstrap | bash -
